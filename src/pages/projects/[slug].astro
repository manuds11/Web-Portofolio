---
import "../../styles/global.css";

export function getStaticPaths() {
  const projects = [
    { slug: "multibody-systems", title: "Multibody Systems", summary: "A general 2D multibody framework that numerically integrates the motion of systems connected by spring-dampers and rigid rods under gravity.", topics: ["Problem and scope", "Physical model", "Solver architecture", "Verification", "Results and media"] },
    { slug: "harmonic-oscillator", title: "Harmonic Oscillator", summary: "The most basic differential equation problem in dynamics, with an analytical solution that can be used to compare numerical integration methods and validate their implementation.", topics: ["Equation of motion", "Integration methods", "Damping", "Energy analysis", "Results and media"] },
    { slug: "unreal-testing", title: "Unreal Engine API Testing", summary: "Basic project to test cinematic visualization of meshes.", topics: ["Test objectives", "3D trajectory", "Quaternion orientation", "Camera behaviour", "Results and media"] },
  ];
  return projects.map((project) => ({ params: { slug: project.slug }, props: { project } }));
}

const { project } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content={project.summary} />
    <title>{project.title} — Manuel Delgado Portfolio</title>
  </head>
  <body>
    <main class="case-study">
      <header class="case-header"><a href="/Web-Portofolio/#projects">← Projects</a><a href="/Web-Portofolio/">Manuel Delgado Portfolio</a></header>
      <section class="case-hero"><p class="section-index">SUBPROJECT</p><h1>{project.title}</h1><p>{project.summary}</p></section>
      <section class="case-outline"><p class="section-index">PROVISIONAL STRUCTURE</p><ol>{project.topics.map((topic, index) => <li><span>{String(index + 1).padStart(2, "0")}</span>{topic}<small>Content to be added</small></li>)}</ol></section>
    </main>
  </body>
</html>
